;; Public facade for the optional, source-linked in-process compiler and JIT.

;; Importing this namespace is what makes the SDK reachable and linked.
(module coil.jit)
(import "coil.compiler.jit_api" :reexport)
