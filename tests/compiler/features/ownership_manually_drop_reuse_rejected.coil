(module ownership_manually_drop_reuse_rejected)

(defstruct Owner [(value i64)])
(impl Drop Owner
  (drop [(self (mut Owner))] (-> void)))

(defn main [] (-> i64)
  (let [held (manually-drop (Owner :value 1))]
    (manually-drop-into-inner held)
    (manually-drop-into-inner held)
    0))
