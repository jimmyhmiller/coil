(module arc_auto_runtime_arraylist_drop)
(import "coil.arc.auto-runtime" :as runtime)

(defn exercise [] (-> i64)
  (let [items (runtime/auto-al-new [i64])]
    42))

(defn main [] (-> i64)
  (let [answer (exercise)]
    (if (= (runtime/auto-arraylist-live-count) 0) answer 1)))
