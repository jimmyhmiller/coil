(module replsession)

(import "coil.primitive" :as primitive)
(import "coil.var")
(import "coil.jit.reload")

(reload-function even [(x i64)] (-> i64))
(reload-function odd [(x i64)] (-> i64))

(defn main [] (-> i64)
  (if (= (even 8) 1) 0 102))
