(module ownership_derive_copy_rejected)
(import "coil.derive" :use *)

(defstruct Resource [(value i64)])

(impl Drop Resource
  (drop [(self (mut Resource))] (-> void) (do)))

(defstruct InvalidCopy [(resource Resource)])

(derive Copy InvalidCopy)

(defn main [] (-> i64)
  0)
