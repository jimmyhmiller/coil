(module void-value-argument-rejected)
(import "coil.primitive" :as p)
(defn write-value [(target (ptr i64))] (-> void) (store! target 42))
(defn identity [T] [(value T)] (-> T) value)
(defn main [] (-> i32)
  (let [target (p/alloc-stack i64)]
    (identity (write-value target)))
  0)
