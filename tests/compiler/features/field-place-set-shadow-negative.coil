(module field-place-set-shadow-negative)

; Two-argument set! can never be called because that arity is assignment syntax.
(defn set! [(x i64) (y i64)] (-> i64)
  (+ x y))

(defn main [] (-> i64)
  0)
