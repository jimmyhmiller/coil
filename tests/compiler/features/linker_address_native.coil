(module linker-address-native)
(import "coil.primitive" :as primitive)

; Linker symbols are target-independent and may refer to functions as well as
; globals. Every function is declared before code generation emits its body.
(defn main [] (-> i64)
  (if (= (primitive/cast i64 (primitive/linker-address main)) 0) 1 0))
