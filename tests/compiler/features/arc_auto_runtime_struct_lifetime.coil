(module arc_auto_runtime_struct_lifetime)
(import "coil.arc.auto-runtime" :as auto)

(defstruct Cell [(value i64)])

(defn main [] (-> i64)
  (do
    (let [first (Cell :value 42)
          second first]
      (.value second))
    (if (= (auto/auto-object-live-count) 0) 42 1)))
