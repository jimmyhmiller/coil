(module code-eq-trait)
(import "coil.primitive" :as primitive)

(defn code-eq-answer [] (-> Code)
  (if (= `same `same)
      (if (= `same `different) `1 `42)
      `2))

(defn main [] (-> i64)
  (code-eq-answer))
