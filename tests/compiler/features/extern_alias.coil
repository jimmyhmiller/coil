(module feature.extern-alias)
(import "coil.primitive" :as primitive)

(extern external-length :as "strlen" :cc c [(ptr i8)] (-> i64))
(extern native-versioned :cc c :as "library.function$1" [] (-> i64))

(defn main [] (-> i64)
  (external-length c"alias"))
