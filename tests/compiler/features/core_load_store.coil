(module core-load-store)
(import "coil.primitive" :as primitive)

(defn main [] (-> i64)
  (let [slot (primitive/alloc-stack Box)]
    (set! (.value slot) 42)
    (if (= (.value slot) 42) 0 1)))

(defstruct Box [(value i64)])
