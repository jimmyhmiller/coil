(module core-load-store)
(import "primitive.coil" :as primitive)

(defn main [] (-> i64)
  (let [slot (primitive/alloc-stack Box)]
    (store! (field slot value) 42)
    (if (= (load (field slot value)) 42) 0 1)))

(defstruct Box [(value i64)])
