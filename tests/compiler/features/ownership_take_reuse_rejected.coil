(module ownership_take_reuse_rejected)

(defstruct Owner [(value i64)])

(impl Drop Owner
  (drop [(self (mut Owner))] (-> void)))

(defn main [] (-> i64)
  (let [(mut owner) (Owner :value 1)
        taken (take! (mut owner))]
    (.value owner)))
