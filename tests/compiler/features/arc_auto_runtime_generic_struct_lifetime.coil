(module arc_auto_runtime_generic_struct_lifetime)
(import "coil.arc.auto-runtime" :as auto)

(defstruct Box [T] [(value T)])

(defn main [] (-> i64)
  (do
    (let [first (Box [i64] :value 42)
          second first]
      (.value second))
    (if (= (auto/auto-object-live-count) 0) 42 1)))
