(module test.struct-reference-field-rejected)

;; A field cannot hold a reference: codegen has no address for one. This used to
;; abort the compiler in codegen ("field on non-pointer") instead of being reported.
(defstruct Held [(v i64)])
(defstruct Holder [(place (ref Held))])

(defn main [] (-> i64) 0)
