(module compiler.features.defalias-duplicate-rejected)
;; One submission cannot bind the same alias twice; only a later retained
;; submission may rebind it.
(defn one [] (-> i64) 1)
(defn two [] (-> i64) 2)
(do (defalias value one) (defalias value two))
(defn main [] (-> i64) (value))
