; `(import "coil.core" :as core)` — Clojure's qualified-only require: the alias works,

; nothing is referred bare.
(module refer-core-qualified)
(import "coil.core" :as core)
(import "coil.primitive" :as primitive)

(defn main [] (-> i64)
  (if (core/empty? "") 0 1))
