(module hermetic.surface.main)
(import "coil.async" :as async)
(import "coil.atomic" :as atomic)
(import "coil.dyn" :as dyn)
(import "coil.match" :as match)
(import "coil.simd" :as simd)
(import "coil.var" :as var)

(defn main [] (-> i64) 0)
