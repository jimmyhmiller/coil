[package]
name = "collision"
entry = "src/main.coil"

[language]
core-providers = ["platform.core", "alternate.core"]

[dependencies]
platform = { path = "../provider" }
alternate = { path = "../provider2" }
