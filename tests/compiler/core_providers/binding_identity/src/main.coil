(module binding.application)
(import "binding.target" :use * :rename [[answer renamed-answer]])
(import "binding.policy" :use *)
(checker check-binding-identity)

(defn main [] (-> i64)
  (renamed-answer))
