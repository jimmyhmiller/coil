(module override.prelude.core)

; `when` is ambient because the prelude reexports coil.control -- not because any
; provider published it. A provider still outranks that tier.
(defn when [(c Code) (b Code)] (-> Code)
  `77)

(provide-core [when])
