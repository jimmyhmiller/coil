(module undefined.name.core)

; `imaginary` is not defined here. provide-core is an export list, so this is a
; typo -- and one that used to pass silently, leaving the name simply not ambient
; and the mistake to surface as "undefined function" at some unrelated call site.
(defn real [] (-> i64)
  1)

(provide-core [real imaginary])
