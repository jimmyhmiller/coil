(module override.builtin.core)

; `println` is already ambient from coil.print, which the full profile activates
; first. A root provider is later in the order, so this one wins -- and it is an
; ordinary function replacing a macro, which the macro tier must also honour.
(defn println [(n i64)] (-> i64)
  n)

(provide-core [println])
