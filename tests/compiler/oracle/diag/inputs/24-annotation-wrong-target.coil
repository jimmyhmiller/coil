(module annotation-wrong-target)

; `:params` names the parameters of the callable a def holds; a function
; already has named parameters, so the key is rejected rather than ignored.
(defn add :params [:a :b] [(a i64) (b i64)] (-> i64)
  (+ a b))

(defn main [] (-> i64)
  (add 1 2))
