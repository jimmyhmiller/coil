(module export-extern-collision)

(defstruct Pair [(a i64) (b i64)])

(defn callback [(value Pair)] (-> i64)
  (.a value))

(export-c [callback :as "callback_c"])

(extern callback_c :cc c [Pair] (-> i64))

(defn main [] (-> i64)
  0)
