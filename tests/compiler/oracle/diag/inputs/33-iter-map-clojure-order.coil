(module diag_iter_map_clojure_order)
(import "coil.primitive" :as primitive)

(defn double [(x i64)] (-> i64)
  (+ x x))

(defn main [] (-> i64)
  (do
    (map (range 0 10) (primitive/fnptr-of double))
    0))
