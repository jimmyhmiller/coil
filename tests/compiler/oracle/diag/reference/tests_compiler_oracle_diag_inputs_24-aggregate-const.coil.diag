error: const 'FAVOURITE' has an aggregate type (Color), which no backend can emit as a static; only scalar consts are supported — return it from a zero-argument function instead
  --> tests/compiler/oracle/diag/inputs/24-aggregate-const.coil:2:18
  |
2 | (const FAVOURITE (Red []))
  |                  ^^^^^^^^
