(module inherent_duplicate_imports)
(import "coil.primitive" :as primitive)
(import "inherent_remote" :use [Remote])
(import "inherent_extension")
(import "inherent_extension_duplicate")

(defn main [] (-> i64)
  (remote-mark (primitive/zeroed Remote)))
