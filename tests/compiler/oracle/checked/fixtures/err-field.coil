(module errtest)
(import "coil.primitive" :as primitive)
(import "coil.alloc" :as alloc)
(defstruct Pt [(x i64) (y i64)])
(defn main [] (-> i64)
  (let [p (primitive/alloc-stack Pt)]
    (primitive/load (primitive/field p z))))
