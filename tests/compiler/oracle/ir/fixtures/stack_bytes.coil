(module oracle.stack-bytes)
(import "coil.primitive" :as p)

(defn main [(argc i32) (argv (ptr (ptr i8)))] (-> i64)
  (let [bytes (p/alloc-stack-bytes (cast u64 argc))]
    (store! bytes (cast u8 42))
    (cast i64 (load bytes))))
