(module hygiene.definition-site-alias-decoy)

(export str-len)

;;; Bound to the alias `st` at the CALL site, and exporting the same name the
;;; provider's template reaches for.
(defn str-len [(s (slice u8))] (-> i64) 999)
