(module hyguser4)
(import "coil.assert" :use *)
(import "coil.io" :use *)

; An ordinary Coil module. Nothing to do with Scheme. It defines its own `=`
; macro, as the language explicitly permits.
(defn = [& (es Code)] (-> Code)
  `true)

(deftest this-must-fail (assert-eq 1 999))
