(module hygiene.definition-site-import)
(import "hygiene.definition-site-import-facade" :use [definition-stdout])

;;; The template's free reference resolves through the provider's imports. The
;;; caller neither imports nor defines `stdout`.
(defn main [] (-> i64)
  (do (definition-stdout) 0))
