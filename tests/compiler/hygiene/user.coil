(module hyguser)
(import "hyglib" :use *)
(import "coil.io" :use *)
; shadow `=` locally with something that always returns true
(defn = [& (es Code)] (-> Code) `true)
(defn main [] (-> i64)
  (print-int (stdout) (eq-check 1 999))
  (print-str (stdout) "\n") 0)
