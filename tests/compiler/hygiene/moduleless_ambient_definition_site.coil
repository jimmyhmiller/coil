;;; A module declaration is optional, but that must not disable qualification of

;;; definition-site references introduced by an ambient/core macro.
(defn main [] (-> i64)
  (do (println "hygiene") 0))
