(module hygiene.definition-site-value)

(import "hygiene.definition-site-value-provider" :use *)

;;; The caller-local `offset` must not capture the template's free reference.
(defn main [] (-> i64)
  (let [offset 100]
    (add-offset 1)))
