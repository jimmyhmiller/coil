(module hyglib)
(import "coil.primitive" :as primitive)
; A macro whose template writes `=`. Hygiene says this `=` resolves in THIS
; module's namespace (coil.core's), regardless of what the caller has in scope.
(defn eq-check [(x Code) (y Code)] (-> Code)
  `(if (= ~x ~y) 1 0))
