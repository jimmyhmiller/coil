(module hygiene.use-site-binder-cannot-capture)

;; MUST FAIL: the caller's source binder cannot capture a definition-template
;; free reference merely because both print as `target`.
(defn template-reference [] (-> Code) `target)

(defn main [] (-> i64)
  (let [target 42]
    (template-reference)))
