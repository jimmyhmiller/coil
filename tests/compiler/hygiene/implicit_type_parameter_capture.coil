(module hygiene.implicit-type-parameter-capture)
(import "coil.primitive" :as primitive)

;;; MUST FAIL: the helper's type reference is not the independently introduced
;;; generic parameter, despite both printing as `T`.
(defn helper-type [] (-> Code) `T)
(defn generate [] (-> Code)
  `(defn generated [T] [(x ~(helper-type))] (-> ~(helper-type)) x))
(meta (generate))

(defn main [] (-> i64) 42)
