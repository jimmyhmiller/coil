(module hyguser3)
(import "hyglib3" :use *)
(import "coil.io" :use *)
(import "coil.primitive" :as primitive)

(defn = [& (es Code)] (-> Code)
  `true)

(defn main [] (-> i64)
  (print-int (stdout) (eq-check3 1 999))
  (print-str (stdout) "\n")
  0)
