(module hygiene.definition-site-type-tag)

(export Tag)

;;; Reached from the provider's template through an ALIAS. This is the shape
;;; `src/stdlib/ui.coil` wanted: a template emitting `(-> AnyView)` for a type it
;;; only knows by its own file-local nickname.
(defstruct Tag [(v i64)])
