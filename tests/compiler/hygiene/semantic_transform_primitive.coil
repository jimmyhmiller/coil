(module hygiene.semantic-transform-primitive)

; Loading the provider registers its semantic transform, but this target neither
; imports nor aliases coil.primitive. The generated primitive heads must retain
; the provider's definition-site binding through the transform fixpoint.
(import "hygiene.semantic-transform-primitive-provider" :use *)

(defn main [] (-> i64)
  (primitive-hygiene-marker))
