(module binding.combined-libraries)
(import "coil.arc.auto")
(import "coil.async-runtime")
(import "coil.serde.derive")

; Source transforms locate generated derive helpers. Their unchanged bodies must
; retain binding normalization when later meta output triggers another tower.
(defn main [] (-> i64)
  0)
