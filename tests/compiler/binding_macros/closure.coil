(module binding-closure)
(import "coil.closure" :use [defclosure])

(defclosure adder [(base i64)] [([x y] (slice i64))] i64 (+ base (+ x y)))

(defn main [] (-> i64)
  (let [f (adder-new 10)
        pair [12 20]
        answer (f pair)]
    (adder-free f)
    (- answer 42)))
