(import "coil.primitive" :as p)

(defn code-sum [(items Code)] (-> Code)
  (let [[a b & rest :as all] items]
    `(+ (+ ~a ~b) ~(p/code-count rest))))

(defn main [] (-> i64)
  (- (code-sum (19 22 9)) 42))
