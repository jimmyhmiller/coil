(module binding-bad-partial-move)

(defstruct Owner [(value i64)])

(impl Drop Owner (drop [(self (mut Owner))] (-> void)))

(defstruct Wrapper [(owner Owner)])

(defn main [] (-> i64)
  (let [(Wrapper :owner owner)
          (Wrapper
            :owner (Owner :value 42))]
    (.value owner)))
