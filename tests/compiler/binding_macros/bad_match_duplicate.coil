(module binding-bad-match-duplicate)

(defstruct Point [(x i64) (y i64)])

(defsum Event (Located [(point Point) (offset i64)]))

(defn main [] (-> i64)
  (match (Located (Point :x 20 :y 22) 0)
    (Located [(Point :x value) value] value)))
