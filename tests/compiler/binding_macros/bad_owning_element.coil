(module binding-bad-owning-element)

(defstruct Owner [(value i64)])

(impl Drop Owner (drop [(self (mut Owner))] (-> void)))

(defn first [(items (slice Owner))] (-> i64)
  (let [[owner] items] (.value owner)))

(defn main [] (-> i64)
  0)
