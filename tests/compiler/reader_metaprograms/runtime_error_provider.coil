(module reader.fixture.runtime-error)
(import "coil.primitive" :as primitive)

(reader-provider "reader.fixture.runtime-error" read-source)

(defn read-source [(_ Code)] (-> Code)
  (primitive/error "reader fixture execution failed"))
