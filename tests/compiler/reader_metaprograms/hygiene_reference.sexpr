(module reader_config_hygiene_reference)

;; Conversely, this template reference must not be captured by the source
;; binder in `main` merely because both display as `x`.
(defn template-reference [] (-> Code) `x)

(defn main [] (-> i64)
  (let [x 42]
    (template-reference)))
