(module reader.fixture.config)
(import "coil.primitive" :as primitive)

(reader-provider "reader.fixture.config" read-source)

(defn read-source [(context Code)] (-> Code)
  (primitive/code-read (primitive/code-nth context 2)
                       `(reader-config :unquote #\, :splice #\@)))
