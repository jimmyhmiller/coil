(module reader.fixture.json-wrapper)

; `[readers]` names this containing module, while the declaration names the
; separate module that owns the implementation function.
(reader-provider "reader.fixture.json" read-json)
