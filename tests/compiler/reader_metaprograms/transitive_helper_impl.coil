(module reader.fixture.transitive-helper-impl)

(export read-through-library)

; Deliberately private. A reader that enters this module through its exported
; API still needs this transitive implementation helper in the read-phase image.
(defn private-answer [] (-> i64)
  42)

(defn read-through-library [] (-> i64)
  (private-answer))
