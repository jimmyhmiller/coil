(module balance-typed-inline)
(import "coil.primitive" :as primitive)

(defn foo [(a i64) (b i64)] (-> i64)
  (primitive/iadd a b))

(defn bar [(x i64)] (-> i64)
  x)

(defn choose [(x i64) (y i64)] (-> i64)
  (foo (bar x y)
       0))
