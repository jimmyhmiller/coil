;; Semantic-fixture target: the call (getf) types as f64 with no float literal

;; at the call site — only inference can flag it.
(module runcmfloat)
(import "coil.primitive" :as primitive)

(defn getf [] (-> f64)
  1.5)

(defn main [] (-> i64)
  (primitive/cast i64 (getf)))
