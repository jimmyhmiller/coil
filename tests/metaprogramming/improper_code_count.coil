(module improper_code_count)
(import "coil.primitive" :as primitive)

(defn improper-count [(modules Code)] (-> Code)
  (do
    (primitive/code-count (primitive/code-cons `a `b))
    `0))

(checker improper-count)

(defn main [] (-> i64) 0)
