(module app)
(import "typecheck" :use *)
(import "wa" :as wa)
(import "wb" :as wb)
(checker check-types)
(defn mk-a [(x wa/Box)] (-> i64) 0)
(defn mk-b [(x wb/Box)] (-> i64) 0)   ; wb/Box → banned; wa/Box → fine
(defn main [] (-> i64) 0)
