(module app)
(import "coil.primitive" :as primitive)
(import "lint" :use *)

(defn f [(x :i64) (y :i64)] (-> :i64)
  (if (< x y) (if (> x (primitive/cast i64 0)) 1 0) (if (= y (primitive/cast i64 0)) 2 3)))

(defn main [] (-> :i64)
  (if (> (f 3 4) (primitive/cast i64 5)) 0 42))
