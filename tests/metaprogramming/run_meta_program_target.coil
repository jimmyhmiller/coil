(module run_meta_program_target)
(import "run_meta_identity" :as identity)

;; The entry module is deliberately ordinary target code. In `--program` mode
;; it is passed to an imported metaprogram as Code, not compiled first.
(defn target-value [] (-> i64) 42)
