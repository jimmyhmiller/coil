(module result-flow-lint-test)
(import "coil.lint.result-flow" :as result-flow-lint)

(defsum DemoError (Bad))

(defn obtain [] (-> (Result i64 DemoError)) (Ok 42))

(defn main [] (-> i64)
  (match (obtain)
    (Err [_] 7)
    (Ok [value] value)))
