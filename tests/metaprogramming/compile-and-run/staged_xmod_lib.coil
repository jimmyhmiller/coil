(module staged_xmod_lib)
(import "coil.primitive" :as primitive)

;;; The stage declaration is attached to THIS module's record; this module also
;;; uses the guest syntax itself.
(defn lib-part [] (-> i64)
  (xpick (7 30)))
