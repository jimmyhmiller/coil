(module isolated_stage_test)
(import "isolated_stage" :use *)

;; isolated-pick exists only inside a generated `(stage ...)` declaration. It
;; is compiled for expansion and must never enter this runtime program.
(defn main [] (-> i64)
  (isolated-pick 42))
