(module staged_xmod_test)
(import "staged_xmod" :use *)
(import "staged_xmod_lib" :use *)

;; The request below lives in THIS module; the entry it names was declared by a
;; `(stage ...)` in staged_xmod_lib's record. 12 + 30 = 42.
(defn main [] (-> i64)
  (+ (xpick (99 12)) (lib-part)))
