(module sprout_output)
(import "coil.primitive" :as primitive)
(import "coil.io" :use *)

(defn square [(x i64)] (-> i64)
  (primitive/imul x x))

(defn main [] (-> i64)
  (do
    (print-int (stdout) (square 7))
    (write-byte (stdout) 10)
    0))
