(module named-constructor-lint-input)

(defsum Event
  (Moved [(x i64) (y i64) (at i64)])
  (Sized [(width i64) (height i64)]))

(defn main [] (-> i64)
  (match (Moved 1 2 3)
    (Moved [x y at] (+ (+ x y) at))
    (Sized [width height] (+ width height))))
