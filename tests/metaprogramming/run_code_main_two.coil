;; Two Code parameters = two input files on the command line.

(module runcodemaintwo)
(import "coil.primitive" :as primitive)

(defn main [(x Code) (y Code)] (-> Code)
  `(first-had ~(primitive/code-count x) second-had ~(primitive/code-count y)))
