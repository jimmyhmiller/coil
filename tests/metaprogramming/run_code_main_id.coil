;; `coil run` on a code->code program: identity. The input file's forms arrive

;; as one Code list; returning it prints it back as a single form.
(module runcodemainid)
(import "coil.primitive" :as primitive)

(defn main [(prog Code)] (-> Code)
  prog)
