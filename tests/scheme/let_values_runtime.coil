(module let-values-runtime)
(import "coil.scheme" :use *)

(define (two n) (values n (+ n 1)))

(defn main [] (-> i64)
  (fixnum-value
    (let-values (((a b) (two 40)))
      (if (= (+ a b) 81) 0 1))))
