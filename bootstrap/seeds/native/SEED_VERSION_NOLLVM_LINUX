linux-x86_64 LLVM-FREE seed, built from source at the commit named below (the seed
necessarily predates its own commit).

source: src/compiler/main_x64.coil (no LLVM backend compiled in)
proof:  no-libLLVM (ldd: libm/libc/ld-linux only) + x64 fixpoint
        (stage2.o==stage3.o, byte-identical) + x64 gate-run 56/56
needs:  a C compiler (cc) to link. That is the entire toolchain — no libLLVM, no Rust.

commit: abcfdbee171ba7d6f7141381810159836a0ce378
built:  2026-08-09T01:29:28Z

⚠ Its STAGE0 is not itself. rebootstrap-nollvm-linux.sh defaults STAGE0 to this
seed, but the stage0 that builds an LLVM-free compiler is itself LLVM-linked, so a
recovery run needs both:

    STAGE0=build/bin/coil COIL_LLVM_LIBDIR=/usr/lib/llvm-21/lib \
      scripts/compiler/rebootstrap-nollvm-linux.sh

Without COIL_LLVM_LIBDIR it stops at "stage0 needs libLLVM but none found", which
reads like a missing dependency of the LLVM-free build rather than of its stage0.

The seed this replaces was stamped 9d6c8ef17fce230d133d3bcc11a2c53a58cf0878, an
object that does not exist in this repository — unauditable, undateable, and unable
to compile the tree that shipped it. gate-seed-provenance.sh now rejects that.
