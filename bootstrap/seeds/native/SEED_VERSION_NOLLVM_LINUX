linux-x86_64 LLVM-free native seed, built from source immediately preceding the
commit that ships this file. It is the preferred fast stage zero for LLVM-free
bootstrap on Linux/x86-64 and needs only the system C toolchain at runtime.

source: src/compiler/main_x64.coil (LLVM-free)
proof:  forced-WASM stage0 + no-libLLVM dependency audit + byte-identical
        stage2/stage3 x64 fixed point + x64 behavioral gate.

If this seed is absent or stale, bootstrap automatically falls back to the
portable WASM seed.
