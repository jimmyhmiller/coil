commit: 2ca4b5fad9cdd142d6aeed5a235a693cd9b05c46
built:  2026-09-07T15:10:07Z
source: src/compiler/main_wasm.coil
target: wasm64-unknown-unknown

Portable compiler using interpreter metaprogramming with ARM64 and x86-64
native object backends. Translated with src/bootstrap/wasm2c.c when a native
seed is unavailable or cannot compile the current sources.

proof:
- wasm-tools validation with memory64 enabled.
- wasm2c translation and native C compilation on macOS ARM64 and Linux ARM64.
- translated compiler checks current main_x64.coil sources on Linux ARM64.
- translated compiler builds a native main_a64.coil stage1 on macOS ARM64.
- portable C host-runtime regression passes ASan/UBSan on both native hosts.

The Linux x86-64 native seeds are not refreshed on this host. Their automatic
fallback is this portable seed; actual Linux x86-64 self-host verification
remains the Linux CI job's responsibility.
