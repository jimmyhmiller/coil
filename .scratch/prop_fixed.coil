(module prop_fixed)
(import "coil.prop" :use *)

; Same property NAME as .scratch/prop_fail.coil's, but now true: the saved
; counterexample must be replayed, observed to pass, and deleted.
(defprop all-ints-small [(a i64)]
  (or (< a 100) (>= a 100)))
