(module runner_compile)
(import "coil.prop.runner" :use *)
(import "coil.assert" :use *)

(deftest runner-compiles
  (assert-eq (size-for 0 100 60) 1)
  (assert-eq (size-for 100 100 60) 60)
  (assert (> (env-int "COIL_PBT_NOT_SET_XYZ" 7) 6)))
